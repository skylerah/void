name: Trigger auto deployment for asdfasdf

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ 📚 ]
    paths:
    - 'server/**'
    - '.github/workflows/asdfasdf-AutoDeployTrigger-e1f4b58c-91f9-4e1f-92d4-14179cccb7a2.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.ASDFASDF_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.ASDFASDF_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.ASDFASDF_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: containerregistryl7guu6wehhy5m.azurecr.io
          registryUsername: ${{ secrets.ASDFASDF_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.ASDFASDF_REGISTRY_PASSWORD }}
          containerAppName: asdfasdf
          resourceGroup: fullstackappa00a
          imageToBuild: containerregistryl7guu6wehhy5m.azurecr.io/asdfasdf:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


